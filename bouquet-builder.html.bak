<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <script>
        if (!sessionStorage.getItem('authenticated')) {
            window.location.href = 'index.html';
        }
    </script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>💐 Your Anniversary Bouquet Builder</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    
    <!-- Three.js for 3D Rendering -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/controls/OrbitControls.js"></script>
    
    <!-- Drag and Drop Library -->
    <script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
    
    <style>
        /* Design System */
        :root {
            --primary-color: #C07F63;
            --primary-dark: #A9684C;
            --primary-light: #D4967C;
            --secondary-color: #EAE0D5;
            --background-cream: #FDF8F0;
            --text-primary: #433A3F;
            --text-secondary: #6B7280;
            --text-accent: #C07F63;
            --success-color: #10B981;
            --warning-color: #F59E0B;
            --border-color: #E5E7EB;
            
            --font-display: 'Playfair Display', serif;
            --font-body: 'Roboto', sans-serif;
            
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            
            --radius-md: 0.5rem;
            --radius-lg: 0.75rem;
            --radius-xl: 1rem;
            --radius-2xl: 1.5rem;
            
            --transition-fast: 0.15s ease;
            --transition-normal: 0.3s ease;
            --transition-slow: 0.5s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-body);
            background: var(--background-cream);
            color: var(--text-primary);
            line-height: 1.6;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        /* Header */
        .hero {
            text-align: center;
            padding: 2rem 0;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: white;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '🌹';
            position: absolute;
            top: 20px;
            right: 10%;
            font-size: 4rem;
            opacity: 0.3;
            animation: float 3s ease-in-out infinite;
        }

        .hero::after {
            content: '🌷';
            position: absolute;
            bottom: 20px;
            left: 10%;
            font-size: 3rem;
            opacity: 0.3;
            animation: float 3s ease-in-out infinite reverse;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .hero h1 {
            font-family: var(--font-display);
            font-size: 2.5rem;
            margin-bottom: 1rem;
            position: relative;
            z-index: 1;
        }

        .hero p {
            font-size: 1.1rem;
            opacity: 0.9;
            max-width: 600px;
            margin: 0 auto;
            position: relative;
            z-index: 1;
        }

        /* Back Navigation */
        .back-nav {
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 100;
            background: white;
            border: 2px solid var(--primary-color);
            border-radius: 50%;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            color: var(--primary-color);
            font-size: 1.2rem;
            transition: all var(--transition-fast);
            box-shadow: var(--shadow-lg);
        }

        .back-nav:hover {
            background: var(--primary-color);
            color: white;
            transform: scale(1.1);
        }

        /* Main Layout */
        .main-layout {
            display: grid;
            grid-template-columns: 1fr 400px;
            gap: 2rem;
            margin-top: 2rem;
            margin-bottom: 2rem;
        }

        /* Flower Selection Panel */
        .flower-panel {
            background: white;
            border-radius: var(--radius-2xl);
            box-shadow: var(--shadow-lg);
            padding: 2rem;
            border: 1px solid var(--border-color);
        }

        .section-header {
            font-family: var(--font-display);
            font-size: 1.5rem;
            color: var(--text-primary);
            margin-bottom: 1.5rem;
            text-align: center;
        }

        /* Category Buttons */
        .flower-categories {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        .category-btn {
            padding: 0.5rem 1rem;
            border: 2px solid var(--border-color);
            border-radius: var(--radius-md);
            background: white;
            color: var(--text-primary);
            font-size: 0.875rem;
            cursor: pointer;
            transition: all var(--transition-fast);
            white-space: nowrap;
        }

        .category-btn:hover {
            border-color: var(--primary-color);
            background: var(--secondary-color);
        }

        .category-btn.active {
            border-color: var(--primary-color);
            background: var(--primary-color);
            color: white;
        }

        /* Color Filter */
        .color-filter-section {
            margin-bottom: 2rem;
            padding-top: 1rem;
            border-top: 1px solid var(--border-color);
        }

        .color-filters {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        .color-btn {
            padding: 0.5rem 0.75rem;
            border: 2px solid var(--border-color);
            border-radius: var(--radius-md);
            background: white;
            color: white;
            font-size: 0.8rem;
            cursor: pointer;
            transition: all var(--transition-fast);
            font-weight: 500;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
            min-width: 70px;
        }

        .color-btn[data-color="all"] {
            background: var(--secondary-color);
            color: var(--text-primary);
            text-shadow: none;
        }

        .color-btn[data-color="white"] {
            color: var(--text-primary) !important;
            text-shadow: none;
        }

        .color-btn:hover {
            transform: translateY(-1px);
            box-shadow: var(--shadow-md);
        }

        .color-btn.active {
            border-color: var(--primary-color);
            border-width: 3px;
            transform: scale(1.05);
        }

        /* Flower Grid */
        .flower-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 1rem;
            max-height: 500px;
            overflow-y: auto;
        }

        .flower-item {
            background: white;
            border: 2px solid var(--border-color);
            border-radius: var(--radius-lg);
            padding: 1rem;
            text-align: center;
            cursor: pointer;
            transition: all var(--transition-normal);
            position: relative;
            overflow: hidden;
        }

        .flower-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(192, 127, 99, 0.1), transparent);
            transition: left 0.6s;
        }

        .flower-item:hover::before {
            left: 100%;
        }

        .flower-item:hover {
            border-color: var(--primary-color);
            transform: translateY(-4px) scale(1.02);
            box-shadow: var(--shadow-lg);
        }

        .flower-item.selected {
            border-color: var(--primary-color);
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: white;
            animation: selectedGlow 2s ease-in-out infinite alternate;
        }

        @keyframes selectedGlow {
            from { box-shadow: 0 0 20px rgba(192, 127, 99, 0.3); }
            to { box-shadow: 0 0 30px rgba(192, 127, 99, 0.6); }
        }

        .flower-emoji {
            font-size: 2rem;
            margin-bottom: 0.5rem;
            display: block;
        }

        .flower-name {
            font-weight: 500;
            margin-bottom: 0.25rem;
            font-size: 0.9rem;
        }

        .flower-meaning {
            font-size: 0.75rem;
            opacity: 0.8;
            margin-bottom: 0.5rem;
        }

        .flower-price {
            font-weight: 700;
            color: var(--success-color);
            font-size: 0.875rem;
        }

        .flower-item.selected .flower-price {
            color: white;
        }

        .flower-count {
            position: absolute;
            top: 5px;
            right: 5px;
            background: var(--primary-color);
            color: white;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.75rem;
            font-weight: 700;
            transform: scale(0);
            transition: transform var(--transition-fast);
        }

        .flower-item.selected .flower-count {
            transform: scale(1);
        }

        /* Bouquet Preview */
        .bouquet-preview {
            background: white;
            border-radius: var(--radius-2xl);
            box-shadow: var(--shadow-lg);
            padding: 2rem;
            border: 1px solid var(--border-color);
            position: sticky;
            top: 2rem;
        }

        .bouquet-canvas {
            width: 100%;
            height: 300px;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            border-radius: var(--radius-xl);
            border: 2px solid var(--border-color);
            position: relative;
            margin-bottom: 1.5rem;
            overflow: hidden;
            cursor: grab;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .bouquet-canvas:active {
            cursor: grabbing;
        }

        .bouquet-canvas.has-flowers {
            background: linear-gradient(135deg, #fef7f0 0%, #fef3ec 100%);
            border-color: var(--primary-light);
        }

        .bouquet-canvas.drag-over {
            border-color: var(--primary-color);
            background: linear-gradient(135deg, #fef7f0 0%, #fef3ec 100%);
            transform: scale(1.02);
            box-shadow: var(--shadow-lg);
        }

        /* Visual Preview Elements */
        .bouquet-visual {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-wrap: wrap;
            gap: 8px;
            padding: 20px;
            z-index: 2;
        }

        .preview-flower {
            display: flex;
            align-items: center;
            justify-content: center;
            background: white;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            font-size: 24px;
            border: 2px solid var(--primary-light);
            box-shadow: var(--shadow-sm);
            animation: fadeIn 0.3s ease-in-out;
            position: relative;
        }

        .preview-flower.multiple {
            transform: scale(1.2);
            border-color: var(--primary-color);
            box-shadow: var(--shadow-md);
        }

        .preview-flower::after {
            content: attr(data-count);
            position: absolute;
            top: -8px;
            right: -8px;
            background: var(--primary-color);
            color: white;
            border-radius: 50%;
            width: 18px;
            height: 18px;
            font-size: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
        }

        .preview-flower[data-count="1"]::after {
            display: none;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }

        .bouquet-arrangement {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 200px;
            height: 200px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-wrap: wrap;
            gap: 5px;
        }

        .bouquet-arrangement.classic {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            align-items: center;
            justify-items: center;
        }

        .bouquet-arrangement.cascade {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            justify-content: center;
            gap: 8px;
            transform: translate(-50%, -50%) rotate(-15deg);
        }

        .bouquet-arrangement.hand-tied {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            align-items: center;
            justify-content: center;
        }

        .bouquet-arrangement.compact {
            width: 150px;
            height: 150px;
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
            align-items: center;
            justify-content: center;
        }

        .empty-state {
            text-align: center;
            color: var(--text-secondary);
        }

        .empty-state .emoji {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .empty-state p {
            font-size: 0.9rem;
        }

        /* Bouquet Summary */
        .bouquet-summary {
            margin-bottom: 1.5rem;
        }

        .summary-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.5rem 0;
            border-bottom: 1px solid var(--border-color);
        }

        .summary-item:last-child {
            border-bottom: none;
        }

        .summary-flower {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .summary-count {
            background: var(--primary-color);
            color: white;
            border-radius: 50%;
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.75rem;
            font-weight: 700;
        }

        /* Bouquet Styles */
        .bouquet-styles {
            margin-bottom: 1.5rem;
        }

        .style-options {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.5rem;
        }

        .style-btn {
            padding: 0.75rem;
            border: 2px solid var(--border-color);
            border-radius: var(--radius-md);
            background: white;
            text-align: center;
            cursor: pointer;
            transition: all var(--transition-fast);
            font-size: 0.875rem;
        }

        .style-btn:hover {
            border-color: var(--primary-color);
            background: var(--secondary-color);
        }

        .style-btn.active {
            border-color: var(--primary-color);
            background: var(--primary-color);
            color: white;
        }

        /* Special Occasion Templates */
        .occasion-templates-section {
            margin-bottom: 1.5rem;
        }

        .occasion-templates {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 0.5rem;
        }

        .template-btn {
            padding: 0.5rem;
            border: 2px solid var(--border-color);
            border-radius: var(--radius-md);
            background: white;
            text-align: center;
            cursor: pointer;
            transition: all var(--transition-fast);
            font-size: 0.8rem;
            white-space: nowrap;
        }

        .template-btn:hover {
            border-color: var(--primary-color);
            background: var(--secondary-color);
            transform: translateY(-2px);
        }

        .template-btn.active {
            border-color: var(--primary-color);
            background: var(--primary-color);
            color: white;
        }

        /* Smart Suggestions */
        .suggestions-panel {
            margin-bottom: 1.5rem;
            background: var(--background-cream);
            border: 1px solid var(--border-color);
            border-radius: var(--radius-lg);
            padding: 1rem;
        }

        .suggestions-panel h4 {
            margin: 0 0 1rem 0;
            color: var(--primary-color);
            text-align: center;
        }

        .suggestion-item {
            animation: fadeIn 0.3s ease;
        }

        .suggestion-flower-btn:hover {
            transform: scale(1.05);
            box-shadow: var(--shadow-sm);
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes slideIn {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }

        @keyframes slideOut {
            from { transform: translateX(0); opacity: 1; }
            to { transform: translateX(100%); opacity: 0; }
        }

        /* Size Guide */
        .bouquet-size-guide {
            margin-bottom: 1.5rem;
        }

        .size-recommendations {
            background: var(--secondary-color);
            padding: 1rem;
            border-radius: var(--radius-md);
            font-size: 0.875rem;
        }

        .size-rec-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.25rem 0;
            border-bottom: 1px solid var(--border-color);
        }

        .size-rec-item:last-child {
            border-bottom: none;
        }

        .size-rec-item.current {
            background: var(--primary-color);
            color: white;
            padding: 0.5rem;
            border-radius: var(--radius-md);
            font-weight: 500;
        }

        /* Action Buttons */
        .action-buttons {
            display: flex;
            gap: 0.75rem;
            flex-wrap: wrap;
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: var(--radius-lg);
            font-weight: 500;
            cursor: pointer;
            transition: all var(--transition-fast);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            flex: 1;
            min-width: 120px;
        }

        .btn-primary {
            background: var(--primary-color);
            color: white;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-1px);
            box-shadow: var(--shadow-md);
        }

        .btn-secondary {
            background: var(--secondary-color);
            color: var(--text-primary);
        }

        .btn-secondary:hover {
            background: var(--border-color);
            transform: translateY(-1px);
            box-shadow: var(--shadow-md);
        }

        /* Price Display */
        .price-display {
            background: linear-gradient(135deg, var(--success-color), #059669);
            color: white;
            padding: 1rem;
            border-radius: var(--radius-lg);
            text-align: center;
            margin-bottom: 1rem;
        }

        .price-amount {
            font-size: 1.5rem;
            font-weight: 700;
            font-family: var(--font-display);
        }

        .price-label {
            font-size: 0.875rem;
            opacity: 0.9;
            margin-bottom: 0.5rem;
        }

        /* Florist Finder */
        .florist-finder {
            background: white;
            border-radius: var(--radius-2xl);
            box-shadow: var(--shadow-lg);
            padding: 2rem;
            margin-top: 2rem;
            border: 1px solid var(--border-color);
        }

        .florist-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-top: 1.5rem;
        }

        .florist-card {
            background: var(--secondary-color);
            border-radius: var(--radius-lg);
            padding: 1.5rem;
            border: 2px solid transparent;
            transition: all var(--transition-normal);
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .florist-card:hover {
            border-color: var(--primary-color);
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .florist-name {
            font-weight: 700;
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
            color: var(--text-primary);
        }

        .florist-address {
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-bottom: 0.75rem;
        }

        .florist-rating {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1rem;
        }

        .stars {
            color: #F59E0B;
        }

        .rating-text {
            font-size: 0.875rem;
            color: var(--text-secondary);
        }

        .florist-btn {
            width: 100%;
            padding: 0.75rem;
            border: none;
            border-radius: var(--radius-md);
            background: var(--primary-color);
            color: white;
            font-weight: 500;
            cursor: pointer;
            transition: all var(--transition-fast);
        }

        .florist-btn:hover {
            background: var(--primary-dark);
            transform: translateY(-1px);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .main-layout {
                grid-template-columns: 1fr;
                gap: 1rem;
            }

            .bouquet-preview {
                position: static;
            }

            .hero h1 {
                font-size: 2rem;
            }

            .hero p {
                font-size: 1rem;
            }

            .flower-grid {
                grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            }

            .flower-categories {
                gap: 0.25rem;
            }

            .category-btn {
                font-size: 0.8rem;
                padding: 0.4rem 0.8rem;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn {
                min-width: auto;
            }
        }

        /* 3D Canvas */
        .canvas-3d {
            width: 100%;
            height: 100%;
            position: absolute;
            top: 0;
            left: 0;
        }

        /* Quick Stats */
        .quick-stats {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 2rem;
            padding: 1.5rem;
            background: rgba(255, 255, 255, 0.8);
            border-radius: var(--radius-xl);
            backdrop-filter: blur(10px);
            border: 1px solid var(--border-color);
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            display: block;
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-color);
            font-family: var(--font-display);
        }

        .stat-label {
            font-size: 0.875rem;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
    </style>
</head>
<body>
    <a href="index.html" class="back-nav" title="Back to Main Site">←</a>
    
    <div class="container">
        <header class="hero">
            <h1>💐 Your Anniversary Bouquet Builder</h1>
            <p>Create the perfect bouquet for your special Providence weekend. Each flower has its own romantic meaning - create something uniquely yours!</p>
        </header>

        <div class="main-layout">
            <!-- Flower Selection Panel -->
            <div class="flower-panel">
                <!-- Categories -->
                <div class="flower-categories-section">
                    <h3 class="section-header">🌸 Flower Categories</h3>
                    <div class="flower-categories">
                        <button class="category-btn active" data-category="all">All Flowers</button>
                        <button class="category-btn" data-category="romantic">💕 Romantic</button>
                        <button class="category-btn" data-category="classic">👑 Classic</button>
                        <button class="category-btn" data-category="exotic">🌺 Exotic</button>
                        <button class="category-btn" data-category="seasonal">🍂 Seasonal</button>
                    </div>
                </div>

                <!-- Special Occasion Templates -->
                <div class="occasion-templates-section">
                    <h4 style="margin-bottom: 1rem; text-align: center;">🎉 Special Occasion Templates</h4>
                    <div class="occasion-templates">
                        <button class="template-btn" data-occasion="anniversary">💕 Anniversary</button>
                        <button class="template-btn" data-occasion="birthday">🎂 Birthday</button>
                        <button class="template-btn" data-occasion="apology">🤲 Apology</button>
                        <button class="template-btn" data-occasion="congratulations">🎊 Congratulations</button>
                        <button class="template-btn" data-occasion="sympathy">🕊️ Sympathy</button>
                        <button class="template-btn" data-occasion="romance">💖 Romance</button>
                        <button class="template-btn" data-occasion="friendship">🤝 Friendship</button>
                        <button class="template-btn" data-occasion="celebration">🥳 Celebration</button>
                    </div>
                </div>

                <!-- Color Filter -->
                <div class="color-filter-section">
                    <h4 style="margin-bottom: 1rem; text-align: center;">🎨 Filter by Color</h4>
                    <div class="color-filters">
                        <button class="color-btn active" data-color="all">All Colors</button>
                        <button class="color-btn" data-color="red" style="background: #ef4444;">❤️ Red</button>
                        <button class="color-btn" data-color="pink" style="background: #ec4899;">💗 Pink</button>
                        <button class="color-btn" data-color="white" style="background: #f8fafc; border: 2px solid #e2e8f0;">🤍 White</button>
                        <button class="color-btn" data-color="yellow" style="background: #eab308;">💛 Yellow</button>
                        <button class="color-btn" data-color="purple" style="background: #a855f7;">💜 Purple</button>
                        <button class="color-btn" data-color="blue" style="background: #3b82f6;">💙 Blue</button>
                        <button class="color-btn" data-color="orange" style="background: #f97316;">🧡 Orange</button>
                    </div>
                </div>

                <!-- Flower Selection -->
                <div class="flower-selection-section">
                    <h3 class="section-header">🌺 Choose Your Flowers</h3>
                    <div class="flower-grid" id="flower-grid">
                        <!-- Flowers will be populated by JavaScript -->
                    </div>
                </div>
            </div>

            <!-- Bouquet Preview -->
            <div class="bouquet-preview">
                <h3 class="section-header">💐 Bouquet Preview</h3>
                <div class="bouquet-canvas" id="bouquet-canvas">
                    <canvas class="canvas-3d" id="canvas-3d"></canvas>
                    <div class="bouquet-visual" id="bouquet-visual">
                        <div class="bouquet-arrangement classic" id="bouquet-arrangement">
                            <!-- Flower previews will be added here -->
                        </div>
                    </div>
                    <div class="empty-state" id="empty-state">
                        <div class="emoji">💐</div>
                        <p>Click on flowers to add them to your bouquet</p>
                        <p style="font-size: 0.8rem; margin-top: 0.5rem; opacity: 0.7;">Right-click to remove • Drag & drop also works</p>
                    </div>
                </div>

                <div class="price-display" id="price-display">
                    <div class="price-label">Estimated Cost</div>
                    <div class="price-amount" id="price-amount">$0</div>
                </div>

                <div class="bouquet-summary" id="bouquet-summary">
                    <!-- Summary will be populated by JavaScript -->
                </div>

                <div class="bouquet-styles">
                    <h4>🎨 Bouquet Style</h4>
                    <div class="style-options">
                        <button class="style-btn active" data-style="classic">Classic Round</button>
                        <button class="style-btn" data-style="cascade">Cascading</button>
                        <button class="style-btn" data-style="hand-tied">Hand-Tied</button>
                        <button class="style-btn" data-style="compact">Compact</button>
                    </div>
                </div>

                <div class="bouquet-size-guide">
                    <h4>📏 Size Guide</h4>
                    <div class="size-recommendations" id="size-recommendations">
                        <!-- Will be populated by JavaScript -->
                    </div>
                </div>

                <!-- Smart Suggestions -->
                <div class="suggestions-panel" id="suggestions-panel" style="display: none;">
                    <h4>💡 Smart Suggestions</h4>
                    <div class="suggestions-content" id="suggestions-content">
                        <!-- Will be populated by JavaScript -->
                    </div>
                </div>

                <div class="action-buttons">
                    <button class="btn btn-primary" id="save-bouquet">
                        💾 Save Bouquet
                    </button>
                    <button class="btn btn-secondary" id="share-bouquet">
                        📤 Share Design
                    </button>
                    <button class="btn btn-secondary" id="reset-bouquet">
                        🔄 Reset
                    </button>
                </div>
            </div>
        </div>

        <!-- Florist Finder Section -->
        <div class="florist-finder">
            <h3 class="section-header">🏪 Local Florists in Providence</h3>
            <p style="text-align: center; margin-bottom: 1rem; color: var(--text-secondary);">Connect with real florists in the Providence area who can create your dream bouquet.</p>
            
            <div class="florist-status-message" id="florist-status-message" style="display: none;"></div>
            
            <div class="florist-grid" id="florist-grid">
                <!-- Florists will be populated by JavaScript -->
            </div>
        </div>

        <!-- Quick Stats -->
        <div class="quick-stats">
            <div class="stat-item">
                <span class="stat-number" id="total-flowers">0</span>
                <span class="stat-label">Total Flowers</span>
            </div>
            <div class="stat-item">
                <span class="stat-number" id="flower-types">0</span>
                <span class="stat-label">Flower Types</span>
            </div>
            <div class="stat-item">
                <span class="stat-number" id="estimated-time">0</span>
                <span class="stat-label">Est. Hours</span>
            </div>
        </div>
    </div>

    <script>
        // Flower Data
        const flowerData = [
            // Romantic Category
            { name: "Red Rose", emoji: "🌹", meaning: "Deep love & passion", category: "romantic", price: 5, color: "red" },
            { name: "Pink Rose", emoji: "�", meaning: "Gratitude & appreciation", category: "romantic", price: 4, color: "pink" },
            { name: "White Rose", emoji: "🤍", meaning: "Pure love & new beginnings", category: "romantic", price: 4, color: "white" },
            { name: "Yellow Rose", emoji: "🌼", meaning: "Friendship & joy", category: "romantic", price: 4, color: "yellow" },
            { name: "Peony", emoji: "🌸", meaning: "Romance & prosperity", category: "romantic", price: 7, color: "pink" },
            { name: "Red Tulip", emoji: "🌷", meaning: "Perfect love", category: "romantic", price: 3, color: "red" },
            { name: "Pink Tulip", emoji: "🌷", meaning: "Caring & affection", category: "romantic", price: 3, color: "pink" },
            
            // Classic Category
            { name: "Pink Carnation", emoji: "🌺", meaning: "Gratitude & love", category: "classic", price: 6, color: "pink" },
            { name: "Red Carnation", emoji: "🌹", meaning: "Deep love", category: "classic", price: 6, color: "red" },
            { name: "White Lily", emoji: "🤍", meaning: "Purity & rebirth", category: "classic", price: 3, color: "white" },
            { name: "Pink Lily", emoji: "🌸", meaning: "Prosperity & abundance", category: "classic", price: 3, color: "pink" },
            { name: "White Chrysanthemum", emoji: "🤍", meaning: "Truth & honesty", category: "classic", price: 4, color: "white" },
            { name: "Yellow Chrysanthemum", emoji: "🌼", meaning: "Joy & optimism", category: "classic", price: 4, color: "yellow" },
            { name: "White Daisy", emoji: "🌼", meaning: "Innocence & new beginnings", category: "classic", price: 2, color: "white" },
            { name: "Baby's Breath", emoji: "☁️", meaning: "Everlasting love", category: "classic", price: 2, color: "white" },
            
            // Exotic Category
            { name: "Purple Orchid", emoji: "🔮", meaning: "Luxury & strength", category: "exotic", price: 12, color: "purple" },
            { name: "White Orchid", emoji: "🤍", meaning: "Elegance & beauty", category: "exotic", price: 12, color: "white" },
            { name: "Bird of Paradise", emoji: "🧡", meaning: "Freedom & magnificence", category: "exotic", price: 15, color: "orange" },
            { name: "Protea", emoji: "🌸", meaning: "Change & transformation", category: "exotic", price: 10, color: "pink" },
            { name: "Red Anthurium", emoji: "❤️", meaning: "Hospitality & happiness", category: "exotic", price: 8, color: "red" },
            { name: "Pink Anthurium", emoji: "💖", meaning: "Love & friendship", category: "exotic", price: 8, color: "pink" },
            { name: "Lotus", emoji: "🪷", meaning: "Purity & enlightenment", category: "exotic", price: 9, color: "pink" },
            
            // Seasonal Category
            { name: "Sunflower", emoji: "🌻", meaning: "Adoration & loyalty", category: "seasonal", price: 4, color: "yellow" },
            { name: "Lavender", emoji: "💜", meaning: "Serenity & grace", category: "seasonal", price: 3, color: "purple" },
            { name: "Blue Hydrangea", emoji: "💙", meaning: "Gratitude & understanding", category: "seasonal", price: 6, color: "blue" },
            { name: "Pink Hydrangea", emoji: "🌸", meaning: "Heartfelt emotion", category: "seasonal", price: 6, color: "pink" },
            { name: "Yellow Daffodil", emoji: "🌼", meaning: "New beginnings", category: "seasonal", price: 3, color: "yellow" },
            { name: "Orange Marigold", emoji: "🧡", meaning: "Passion & creativity", category: "seasonal", price: 2, color: "orange" },
            { name: "Red Poinsettia", emoji: "🌹", meaning: "Celebration & success", category: "seasonal", price: 5, color: "red" }
        ];

        // State
        let selectedFlowers = {};
        let currentCategory = 'all';
        let currentColor = 'all';
        let currentStyle = 'classic';

        // DOM Elements
        const flowerGrid = document.getElementById('flower-grid');
        const bouquetCanvas = document.getElementById('bouquet-canvas');
        const emptyState = document.getElementById('empty-state');
        const bouquetSummary = document.getElementById('bouquet-summary');
        const priceAmount = document.getElementById('price-amount');
        const categoryBtns = document.querySelectorAll('.category-btn');
        const colorBtns = document.querySelectorAll('.color-btn');
        const styleBtns = document.querySelectorAll('.style-btn');
        const floristGrid = document.getElementById('florist-grid');
        const floristStatus = document.getElementById('florist-status-message');

        // Initialize
        document.addEventListener('DOMContentLoaded', function() {
            renderFlowers();
            renderFlorists();
            updateSummary();
            setupEventListeners();
        });

        // Render flowers
        function renderFlowers() {
            let filteredFlowers = flowerData;
            
            // Filter by category
            if (currentCategory !== 'all') {
                filteredFlowers = filteredFlowers.filter(flower => flower.category === currentCategory);
            }
            
            // Filter by color
            if (currentColor !== 'all') {
                filteredFlowers = filteredFlowers.filter(flower => flower.color === currentColor);
            }
            
            const flowerElements = filteredFlowers.map(flower => {
                const count = selectedFlowers[flower.name] || 0;
                return `
                    <div class="flower-item ${count > 0 ? 'selected' : ''}" 
                         data-flower="${flower.name}" 
                         draggable="true"
                         onclick="selectFlower('${flower.name}')"
                         oncontextmenu="event.preventDefault(); removeFlower('${flower.name}');"
                         title="Left click to add, right click to remove">
                        <span class="flower-emoji">${flower.emoji}</span>
                        <div class="flower-name">${flower.name}</div>
                        <div class="flower-meaning">${flower.meaning}</div>
                        <div class="flower-price">$${flower.price}</div>
                        <div class="flower-count">${count}</div>
                    </div>
                `;
            }).join('');
            
            flowerGrid.innerHTML = flowerElements || '<p style="text-align: center; color: var(--text-secondary); grid-column: 1/-1;">No flowers match your filters</p>';
        }

        // Select flower
        function selectFlower(flowerName) {
            if (selectedFlowers[flowerName]) {
                selectedFlowers[flowerName]++;
            } else {
                selectedFlowers[flowerName] = 1;
            }
            renderFlowers();
            updateSummary();
        }

        // Remove flower (right-click or special function)
        function removeFlower(flowerName) {
            if (selectedFlowers[flowerName]) {
                selectedFlowers[flowerName]--;
                if (selectedFlowers[flowerName] <= 0) {
                    delete selectedFlowers[flowerName];
                }
                renderFlowers();
                updateSummary();
            }
        }

        // Update summary
        function updateSummary() {
            const flowers = Object.entries(selectedFlowers).map(([flowerName, count]) => {
                const flower = flowerData.find(f => f.name === flowerName);
                return { flower, count };
            });

            const summaryElements = flowers.map(({ flower, count }) => `
                <div class="summary-item">
                    <div class="summary-flower">
                        <span>${flower.emoji}</span>
                        <span>${flower.name}</span>
                    </div>
                    <div class="summary-count">${count}</div>
                </div>
            `).join('');

            bouquetSummary.innerHTML = summaryElements;

            // Update price
            const totalPrice = flowers.reduce((sum, { flower, count }) => sum + (flower.price * count), 0);
            priceAmount.textContent = `$${totalPrice}`;

            // Update stats
            const totalFlowers = Object.values(selectedFlowers).reduce((sum, count) => sum + count, 0);
            const flowerTypes = Object.keys(selectedFlowers).length;
            const estimatedTime = Math.ceil(totalFlowers / 10); // Rough estimate

            document.getElementById('total-flowers').textContent = totalFlowers;
            document.getElementById('flower-types').textContent = flowerTypes;
            document.getElementById('estimated-time').textContent = estimatedTime;

            // Update size recommendations
            updateSizeGuide(totalFlowers, totalPrice);

            // Update smart suggestions
            updateSmartSuggestions();

            // Update canvas state and visual preview
            if (totalFlowers > 0) {
                bouquetCanvas.classList.add('has-flowers');
                emptyState.style.display = 'none';
                updateBouquetPreview();
            } else {
                bouquetCanvas.classList.remove('has-flowers');
                emptyState.style.display = 'block';
                document.getElementById('bouquet-visual').style.display = 'none';
            }
        }

        // Update size guide
        function updateSizeGuide(totalFlowers, totalPrice) {
            const sizeGuide = [
                { name: "Petite Bouquet", flowers: "6-10", price: "$15-35", description: "Perfect for a sweet gesture" },
                { name: "Small Bouquet", flowers: "11-15", price: "$36-60", description: "Ideal for dates & dinners" },
                { name: "Medium Bouquet", flowers: "16-25", price: "$61-100", description: "Great for anniversaries" },
                { name: "Large Bouquet", flowers: "26-35", price: "$101-150", description: "Special celebrations" },
                { name: "Grand Bouquet", flowers: "36+", price: "$151+", description: "Truly spectacular!" }
            ];

            const sizeElements = sizeGuide.map((size, index) => {
                const [minFlowers, maxFlowers] = size.flowers.includes('+') 
                    ? [parseInt(size.flowers), Infinity] 
                    : size.flowers.split('-').map(n => parseInt(n));
                
                const isCurrent = totalFlowers >= minFlowers && totalFlowers <= maxFlowers;
                
                return `
                    <div class="size-rec-item ${isCurrent ? 'current' : ''}">
                        <div>
                            <strong>${size.name}</strong><br>
                            <span style="opacity: 0.8">${size.description}</span>
                        </div>
                        <div style="text-align: right">
                            ${size.flowers} flowers<br>
                            <span style="opacity: 0.8">${size.price}</span>
                        </div>
                    </div>
                `;
            }).join('');

            document.getElementById('size-recommendations').innerHTML = sizeElements;
        }

        // Update bouquet preview
        function updateBouquetPreview() {
            const bouquetVisual = document.getElementById('bouquet-visual');
            const bouquetArrangement = document.getElementById('bouquet-arrangement');
            
            // Show the visual preview
            bouquetVisual.style.display = 'block';
            
            // Update arrangement style
            bouquetArrangement.className = `bouquet-arrangement ${currentStyle}`;
            
            // Create flower preview elements
            const previewElements = [];
            
            Object.entries(selectedFlowers).forEach(([flowerName, count]) => {
                const flower = flowerData.find(f => f.name === flowerName);
                if (flower) {
                    // Add multiple instances for count > 1
                    for (let i = 0; i < Math.min(count, 8); i++) { // Limit to 8 visible flowers per type
                        const flowerElement = document.createElement('div');
                        flowerElement.className = `preview-flower ${count > 1 ? 'multiple' : ''}`;
                        flowerElement.setAttribute('data-count', count);
                        flowerElement.textContent = flower.emoji;
                        flowerElement.title = `${flower.name} (${count})`;
                        previewElements.push(flowerElement);
                    }
                }
            });
            
            // Clear and add new elements
            bouquetArrangement.innerHTML = '';
            previewElements.forEach(element => {
                bouquetArrangement.appendChild(element);
            });
            
            // Add some animation staggering
            previewElements.forEach((element, index) => {
                element.style.animationDelay = `${index * 0.1}s`;
            });
        }

        // Setup event listeners
        function setupEventListeners() {
            // Category buttons
            categoryBtns.forEach(btn => {
                btn.addEventListener('click', function() {
                    categoryBtns.forEach(b => b.classList.remove('active'));
                    this.classList.add('active');
                    currentCategory = this.dataset.category;
                    renderFlowers();
                });
            });

            // Color buttons
            colorBtns.forEach(btn => {
                btn.addEventListener('click', function() {
                    colorBtns.forEach(b => b.classList.remove('active'));
                    this.classList.add('active');
                    currentColor = this.dataset.color;
                    renderFlowers();
                });
            });

            // Style buttons
            styleBtns.forEach(btn => {
                btn.addEventListener('click', function() {
                    styleBtns.forEach(b => b.classList.remove('active'));
                    this.classList.add('active');
                    currentStyle = this.dataset.style;
                    updateBouquetPreview(); // Update preview when style changes
                });
            });

            // Template buttons
            const templateBtns = document.querySelectorAll('.template-btn');
            templateBtns.forEach(btn => {
                btn.addEventListener('click', function() {
                    templateBtns.forEach(b => b.classList.remove('active'));
                    this.classList.add('active');
                    const occasion = this.dataset.occasion;
                    applyOccasionTemplate(occasion);
                });
            });

            // Action buttons
            document.getElementById('save-bouquet').addEventListener('click', saveBouquet);
            document.getElementById('share-bouquet').addEventListener('click', shareBouquet);
            document.getElementById('reset-bouquet').addEventListener('click', resetBouquet);

            // Drag and drop
            setupDragAndDrop();
        }

        // Drag and drop functionality
        function setupDragAndDrop() {
            bouquetCanvas.addEventListener('dragover', handleDragOver);
            bouquetCanvas.addEventListener('drop', handleDrop);
            bouquetCanvas.addEventListener('dragleave', handleDragLeave);

            // Add drag start event to flower items
            document.addEventListener('dragstart', function(e) {
                if (e.target.classList.contains('flower-item')) {
                    e.dataTransfer.setData('text/plain', e.target.dataset.flower);
                }
            });
        }

        function handleDragOver(e) {
            e.preventDefault();
            e.dataTransfer.dropEffect = 'move';
            bouquetCanvas.classList.add('drag-over');
        }

        function handleDrop(e) {
            e.preventDefault();
            const flowerName = e.dataTransfer.getData('text/plain');
            if (flowerName) {
                selectFlower(flowerName);
            }
            bouquetCanvas.classList.remove('drag-over');
        }

        function handleDragLeave(e) {
            bouquetCanvas.classList.remove('drag-over');
        }

        // Action functions
        function saveBouquet() {
            const bouquetData = {
                flowers: selectedFlowers,
                style: currentStyle,
                timestamp: new Date().toISOString()
            };
            
            localStorage.setItem('savedBouquet', JSON.stringify(bouquetData));
            alert('💾 Bouquet saved! You can reload this page to restore your design.');
        }

        function shareBouquet() {
            const totalFlowers = Object.values(selectedFlowers).reduce((sum, count) => sum + count, 0);
            const flowerTypes = Object.keys(selectedFlowers).length;
            const totalPrice = Object.entries(selectedFlowers).reduce((sum, [name, count]) => {
                const flower = flowerData.find(f => f.name === name);
                return sum + (flower.price * count);
            }, 0);

            const shareText = `Check out my custom anniversary bouquet! 💐
Flowers: ${totalFlowers} (${flowerTypes} types)
Style: ${currentStyle}
Total: $${totalPrice}

Created with the Providence Anniversary Bouquet Builder`;

            if (navigator.share) {
                navigator.share({
                    title: 'My Anniversary Bouquet',
                    text: shareText,
                    url: window.location.href
                });
            } else {
                navigator.clipboard.writeText(shareText).then(() => {
                    alert('📤 Bouquet details copied to clipboard!');
                });
            }
        }

        function resetBouquet() {
            if (confirm('Are you sure you want to reset your bouquet? This will remove all selected flowers.')) {
                selectedFlowers = {};
                
                // Clear active template selection
                const templateBtns = document.querySelectorAll('.template-btn');
                templateBtns.forEach(btn => btn.classList.remove('active'));
                
                renderFlowers();
                updateSummary();
            }
        }

        // Load saved bouquet
        function loadSavedBouquet() {
            const saved = localStorage.getItem('savedBouquet');
            if (saved) {
                const bouquetData = JSON.parse(saved);
                selectedFlowers = bouquetData.flowers;
                currentStyle = bouquetData.style;
                
                // Update style buttons
                styleBtns.forEach(btn => {
                    btn.classList.toggle('active', btn.dataset.style === currentStyle);
                });
                
                renderFlowers();
                updateSummary();
            }
        }

        // Render florists
        function renderFlorists() {
            const floristData = [
                {
                    name: "Petals & Stems",
                    address: "123 Federal Hill, Providence, RI",
                    rating: 4.8,
                    reviews: 124,
                    phone: "(401) 555-0123",
                    email: "orders@petalsstems.com"
                },
                {
                    name: "Providence Flower Shop",
                    address: "456 Thayer Street, Providence, RI",
                    rating: 4.6,
                    reviews: 89,
                    phone: "(401) 555-0456",
                    email: "info@providenceflowers.com"
                },
                {
                    name: "Blooms & Blossoms",
                    address: "789 Hope Street, Providence, RI",
                    rating: 4.9,
                    reviews: 156,
                    phone: "(401) 555-0789",
                    email: "orders@bloomsblossoms.com"
                }
            ];

            const floristElements = floristData.map((florist, index) => `
                <div class="florist-card">
                    <div class="florist-name">${florist.name}</div>
                    <div class="florist-address">${florist.address}</div>
                    <div class="florist-rating">
                        <span class="stars">${'★'.repeat(Math.floor(florist.rating))}${florist.rating % 1 ? '☆' : ''}</span>
                        <span class="rating-text">${florist.rating} (${florist.reviews} reviews)</span>
                    </div>
                    <div class="florist-contact" style="font-size: 0.85rem; color: var(--text-secondary); margin-bottom: 1rem;">
                        📞 ${florist.phone}<br>
                        📧 ${florist.email}
                    </div>
                    <button class="florist-btn" onclick="contactFlorist(${index})">
                        � Email Bouquet Order
                    </button>
                </div>
            `).join('');

            floristGrid.innerHTML = floristElements;
        }

        function contactFlorist(index) {
            const floristData = [
                { name: "Petals & Stems", phone: "(401) 555-0123", email: "orders@petalsstems.com" },
                { name: "Providence Flower Shop", phone: "(401) 555-0456", email: "info@providenceflowers.com" },
                { name: "Blooms & Blossoms", phone: "(401) 555-0789", email: "orders@bloomsblossoms.com" }
            ];

            const florist = floristData[index];
            const totalFlowers = Object.values(selectedFlowers).reduce((sum, count) => sum + count, 0);
            
            if (totalFlowers === 0) {
                alert('Please select some flowers for your bouquet first!');
                return;
            }

            // Show contact options
            const contactChoice = confirm(
                `Contact ${florist.name}?\n\n` +
                `Phone: ${florist.phone}\n` +
                `Email: ${florist.email}\n\n` +
                `Click OK to send email, Cancel to just show contact info.`
            );

            if (contactChoice) {
                emailFlorist(florist);
            } else {
                // Just show contact info
                alert(`Contact ${florist.name}:\n\nPhone: ${florist.phone}\nEmail: ${florist.email}`);
            }
        }

        function emailFlorist(florist) {
            const totalFlowers = Object.values(selectedFlowers).reduce((sum, count) => sum + count, 0);
            const flowerTypes = Object.keys(selectedFlowers).length;
            const totalPrice = Object.entries(selectedFlowers).reduce((sum, [name, count]) => {
                const flower = flowerData.find(f => f.name === name);
                return sum + (flower.price * count);
            }, 0);

            // Create detailed flower list
            const flowerList = Object.entries(selectedFlowers).map(([flowerName, count]) => {
                const flower = flowerData.find(f => f.name === flowerName);
                return `• ${count}x ${flower.name} (${flower.meaning}) - $${flower.price} each`;
            }).join('\n');

            // Generate email content
            const subject = `Custom Anniversary Bouquet Order - ${new Date().toLocaleDateString()}`;
            
            const body = `Dear ${florist.name},

I would like to order a custom anniversary bouquet with the following specifications:

BOUQUET DETAILS:
• Style: ${currentStyle.charAt(0).toUpperCase() + currentStyle.slice(1)}
• Total Flowers: ${totalFlowers}
• Flower Types: ${flowerTypes}
• Estimated Cost: $${totalPrice}

FLOWER SELECTION:
${flowerList}

SPECIAL OCCASION:
This bouquet is for our anniversary celebration in Providence. We're looking for something romantic and meaningful, with each flower chosen for its symbolic meaning.

Please let me know:
1. If you can create this bouquet
2. Your pricing and availability
3. Any suggested substitutions if certain flowers aren't available
4. Pickup/delivery options

Thank you for your time and expertise!

Best regards,
[Your Name]
[Your Phone Number]
[Your Email]

---
Created with the Providence Anniversary Bouquet Builder
${window.location.href}`;

            // Try to open default email client
            const mailtoLink = `mailto:${florist.email}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
            
            try {
                window.location.href = mailtoLink;
                
                // Show success message with fallback options
                setTimeout(() => {
                    alert(
                        `✅ Opening email to ${florist.name}...\n\n` +
                        `If your email client didn't open, you can:\n` +
                        `1. Copy the details from the next dialog\n` +
                        `2. Manually email: ${florist.email}\n` +
                        `3. Call: ${florist.phone}`
                    );
                }, 1000);
                
            } catch (error) {
                // Fallback: show email content to copy
                showEmailFallback(florist, subject, body);
            }
        }

        function showEmailFallback(florist, subject, body) {
            // Create a modal-like dialog with the email content
            const modal = document.createElement('div');
            modal.style.cssText = `
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: rgba(0,0,0,0.8);
                display: flex;
                align-items: center;
                justify-content: center;
                z-index: 1000;
            `;
            
            const content = document.createElement('div');
            content.style.cssText = `
                background: white;
                padding: 2rem;
                border-radius: 1rem;
                max-width: 600px;
                max-height: 80vh;
                overflow-y: auto;
                position: relative;
            `;
            
            content.innerHTML = `
                <h3 style="margin-bottom: 1rem; color: var(--primary-color);">
                    📧 Email to ${florist.name}
                </h3>
                <p style="margin-bottom: 1rem; color: var(--text-secondary);">
                    Copy this content and send to: <strong>${florist.email}</strong>
                </p>
                <div style="margin-bottom: 1rem;">
                    <strong>Subject:</strong>
                    <textarea readonly style="width: 100%; margin-top: 0.5rem; padding: 0.5rem; border: 1px solid #ddd; border-radius: 4px;">${subject}</textarea>
                </div>
                <div style="margin-bottom: 1rem;">
                    <strong>Message:</strong>
                    <textarea readonly style="width: 100%; height: 300px; margin-top: 0.5rem; padding: 0.5rem; border: 1px solid #ddd; border-radius: 4px; font-family: monospace;">${body}</textarea>
                </div>
                <div style="display: flex; gap: 1rem; justify-content: flex-end;">
                    <button onclick="copyEmailContent('${subject}', '${body.replace(/'/g, "\\'")}'); this.textContent='✅ Copied!'" 
                            style="padding: 0.5rem 1rem; background: var(--primary-color); color: white; border: none; border-radius: 4px; cursor: pointer;">
                        📋 Copy All
                    </button>
                    <button onclick="this.parentElement.parentElement.parentElement.remove()" 
                            style="padding: 0.5rem 1rem; background: #6b7280; color: white; border: none; border-radius: 4px; cursor: pointer;">
                        Close
                    </button>
                </div>
            `;
            
            modal.appendChild(content);
            document.body.appendChild(modal);
            
            // Close on background click
            modal.addEventListener('click', (e) => {
                if (e.target === modal) {
                    modal.remove();
                }
            });
        }

        function copyEmailContent(subject, body) {
            const fullContent = `Subject: ${subject}\n\n${body}`;
            navigator.clipboard.writeText(fullContent).then(() => {
                console.log('Email content copied to clipboard');
            }).catch(err => {
                console.error('Failed to copy: ', err);
                // Fallback for older browsers
                const textArea = document.createElement('textarea');
                textArea.value = fullContent;
                document.body.appendChild(textArea);
                textArea.select();
                document.execCommand('copy');
                document.body.removeChild(textArea);
            });
        }

        // Special Occasion Templates
        function applyOccasionTemplate(occasion) {
            // Clear current selection
            selectedFlowers = {};
            
            // Define template recommendations
            const templates = {
                anniversary: [
                    { name: "Red Rose", count: 12 },
                    { name: "Baby's Breath", count: 3 },
                    { name: "Pink Rose", count: 6 }
                ],
                birthday: [
                    { name: "Sunflower", count: 5 },
                    { name: "Pink Carnation", count: 4 },
                    { name: "Yellow Rose", count: 6 },
                    { name: "White Daisy", count: 3 }
                ],
                apology: [
                    { name: "White Rose", count: 9 },
                    { name: "Pink Lily", count: 3 },
                    { name: "Baby's Breath", count: 4 }
                ],
                congratulations: [
                    { name: "Yellow Rose", count: 8 },
                    { name: "Sunflower", count: 3 },
                    { name: "Orange Marigold", count: 5 },
                    { name: "White Daisy", count: 2 }
                ],
                sympathy: [
                    { name: "White Lily", count: 6 },
                    { name: "White Chrysanthemum", count: 4 },
                    { name: "Baby's Breath", count: 5 }
                ],
                romance: [
                    { name: "Red Rose", count: 18 },
                    { name: "Pink Rose", count: 6 },
                    { name: "Peony", count: 3 }
                ],
                friendship: [
                    { name: "Yellow Rose", count: 12 },
                    { name: "Sunflower", count: 4 },
                    { name: "White Daisy", count: 6 }
                ],
                celebration: [
                    { name: "Pink Rose", count: 6 },
                    { name: "Yellow Rose", count: 6 },
                    { name: "Orange Marigold", count: 4 },
                    { name: "Sunflower", count: 2 }
                ]
            };

            // Apply the template
            const template = templates[occasion];
            if (template) {
                template.forEach(flower => {
                    selectedFlowers[flower.name] = flower.count;
                });
                
                // Update displays
                updateBouquetPreview();
                updateBouquetSummary();
                updateSizeGuide();
                updateSmartSuggestions();
                
                // Show success message
                showNotification(`🎉 Applied ${occasion} template! Your bouquet is ready to customize.`);
            }
        }

        // Smart Suggestions System
        function updateSmartSuggestions() {
            const suggestionsPanel = document.getElementById('suggestions-panel');
            const suggestionsContent = document.getElementById('suggestions-content');
            
            // Get current bouquet analysis
            const totalFlowers = Object.values(selectedFlowers).reduce((sum, count) => sum + count, 0);
            const selectedFlowerNames = Object.keys(selectedFlowers);
            const colors = [...new Set(selectedFlowerNames.map(name => {
                const flower = flowerData.find(f => f.name === name);
                return flower ? flower.color : '';
            }).filter(Boolean))];
            
            let suggestions = [];
            
            // If no flowers selected, show getting started suggestions
            if (totalFlowers === 0) {
                suggestions.push({
                    type: 'getting-started',
                    title: '🌸 Getting Started',
                    message: 'Try selecting a special occasion template above, or start by choosing your favorite flowers from the grid below!'
                });
            } else {
                // Color harmony suggestions
                if (colors.length === 1) {
                    const currentColor = colors[0];
                    const complementaryFlowers = flowerData.filter(f => 
                        f.color !== currentColor && 
                        !selectedFlowerNames.includes(f.name) &&
                        getComplementaryColors(currentColor).includes(f.color)
                    );
                    
                    if (complementaryFlowers.length > 0) {
                        suggestions.push({
                            type: 'color-harmony',
                            title: '🎨 Color Harmony',
                            message: `Add ${complementaryFlowers.slice(0, 2).map(f => f.name).join(' or ')} to complement your ${currentColor} theme.`,
                            flowers: complementaryFlowers.slice(0, 2)
                        });
                    }
                }
                
                // Size suggestions
                if (totalFlowers < 8) {
                    suggestions.push({
                        type: 'size-suggestion',
                        title: '📏 Size Tip',
                        message: 'Consider adding more flowers for a fuller bouquet. 12-18 flowers create a beautiful, substantial arrangement.'
                    });
                } else if (totalFlowers > 25) {
                    suggestions.push({
                        type: 'size-suggestion',
                        title: '⚡ Size Tip',
                        message: 'You have a wonderfully full bouquet! Consider if you want to keep it this abundant or streamline for elegance.'
                    });
                }
                
                // Texture suggestions
                const hasFillerFlowers = selectedFlowerNames.some(name => 
                    ['Baby\'s Breath', 'White Daisy'].includes(name)
                );
                
                if (!hasFillerFlowers && totalFlowers > 5) {
                    suggestions.push({
                        type: 'texture-suggestion',
                        title: '✨ Texture Enhancement',
                        message: 'Add Baby\'s Breath or White Daisies to create beautiful texture and fill gaps in your arrangement.',
                        flowers: flowerData.filter(f => ['Baby\'s Breath', 'White Daisy'].includes(f.name))
                    });
                }
                
                // Occasion-specific suggestions
                const roseCount = selectedFlowerNames.filter(name => name.includes('Rose')).length;
                if (roseCount > 2) {
                    suggestions.push({
                        type: 'occasion-suggestion',
                        title: '💕 Perfect for Romance',
                        message: 'Your rose-heavy bouquet is perfect for anniversaries, dates, or expressing deep love!'
                    });
                }
            }
            
            // Display suggestions
            if (suggestions.length > 0) {
                suggestionsPanel.style.display = 'block';
                suggestionsContent.innerHTML = suggestions.map(suggestion => {
                    let content = `
                        <div class="suggestion-item" style="margin-bottom: 1rem; padding: 1rem; background: #f8f9fa; border-radius: 8px; border-left: 4px solid var(--primary-color);">
                            <h5 style="margin: 0 0 0.5rem 0; color: var(--primary-color);">${suggestion.title}</h5>
                            <p style="margin: 0 0 0.5rem 0; color: var(--text-primary);">${suggestion.message}</p>
                    `;
                    
                    if (suggestion.flowers) {
                        content += `
                            <div style="display: flex; gap: 0.5rem; flex-wrap: wrap;">
                                ${suggestion.flowers.map(flower => `
                                    <button class="suggestion-flower-btn" data-flower="${flower.name}" 
                                            style="padding: 0.25rem 0.5rem; background: var(--secondary-color); border: 1px solid var(--border-color); border-radius: 4px; cursor: pointer; font-size: 0.8rem;">
                                        ${flower.emoji} ${flower.name}
                                    </button>
                                `).join('')}
                            </div>
                        `;
                    }
                    
                    content += '</div>';
                    return content;
                }).join('');
                
                // Add click handlers for suggestion flowers
                suggestionsContent.querySelectorAll('.suggestion-flower-btn').forEach(btn => {
                    btn.addEventListener('click', function() {
                        const flowerName = this.dataset.flower;
                        addFlowerToBouquet(flowerName);
                        this.style.background = 'var(--success-color)';
                        this.style.color = 'white';
                        this.textContent = '✅ Added';
                        setTimeout(() => updateSmartSuggestions(), 1000);
                    });
                });
            } else {
                suggestionsPanel.style.display = 'none';
            }
        }

        // Helper function to get complementary colors
        function getComplementaryColors(color) {
            const colorMap = {
                'red': ['white', 'pink'],
                'pink': ['white', 'red'],
                'white': ['red', 'pink', 'yellow'],
                'yellow': ['purple', 'white'],
                'purple': ['yellow', 'white'],
                'blue': ['white', 'yellow'],
                'orange': ['purple', 'white']
            };
            return colorMap[color] || ['white'];
        }

        // Helper function to add flower to bouquet
        function addFlowerToBouquet(flowerName) {
            if (selectedFlowers[flowerName]) {
                selectedFlowers[flowerName]++;
            } else {
                selectedFlowers[flowerName] = 1;
            }
            updateBouquetPreview();
            updateBouquetSummary();
            updateSizeGuide();
        }

        // Notification system
        function showNotification(message) {
            const notification = document.createElement('div');
            notification.style.cssText = `
                position: fixed;
                top: 20px;
                right: 20px;
                background: var(--success-color);
                color: white;
                padding: 1rem 1.5rem;
                border-radius: 8px;
                box-shadow: var(--shadow-lg);
                z-index: 1000;
                font-family: var(--font-body);
                max-width: 300px;
                animation: slideIn 0.3s ease;
            `;
            notification.textContent = message;
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.style.animation = 'slideOut 0.3s ease';
                setTimeout(() => notification.remove(), 300);
            }, 3000);
        }

        // Load saved bouquet on page load
        loadSavedBouquet();

        // Initialize smart suggestions
        updateSmartSuggestions();
    </script>
</body>
</html>